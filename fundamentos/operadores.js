function operadores() {
    // ==

    // Se va a encargar de verificar que el tipo de dato
    // sea el valor sobre el cual estamos comparando
    // y además el tipo de dato.
    var edadMinimaVotar = 18;

    if (edadMinimaVotar == '18') {
        console.log('Tiene la edad minima de 18 para votar');
    } else {
        console.log('no tiene la edad minima para votar');
    }

    /* Pendientes de explicar */
    /* !=, >, <, >=, <= */
};